{
  "name": "@darkflare/wrapper",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "publish:canary": "turbo run publish:canary",
    "publish:patch": "turbo run publish:patch",
    "publish:minor": "turbo run publish:minor",
    "publish:major": "turbo run publish:major"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.2.9"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=8"
  }
}
